# Copy to .env and fill in real values. Never commit secrets.

# --- Public origins -----------------------------------------------------
# API_URL is baked into the frontend bundle: the externally reachable origin.
API_URL=http://localhost:8080
# Public frontend origin (also used to build the OAuth callback).
APP_BASE_URL=http://localhost:8080
# Optional alias for APP_BASE_URL consumed by rxconfig.py.
DEPLOY_URL=

# --- Ports -------------------------------------------------------------
# Public port (hosts usually inject PORT automatically).
PORT=8080
# Internal Reflex backend port.
BACKEND_PORT=8000
# Set to 1 to re-export the frontend at boot using API_URL.
REFLEX_EXPORT_ON_START=0

# --- Supabase ----------------------------------------------------------
SUPABASE_URL=
SUPABASE_KEY=

# --- Google OAuth ------------------------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional: pin the exact callback URL (highest priority).
OAUTH_REDIRECT_URL=
OAUTH_CALLBACK_URL=

# --- OpenAI ------------------------------------------------------------
OPENAI_API_KEY=

# --- Database (optional) ----------------------------------------------
REFLEX_DB_URL=
