# Copy to .env and fill in real values. NEVER commit .env.
# Every variable below is read at runtime by existing app code.

# --- Runtime / networking -----------------------------------------------------
# Public origin of the deployment. Baked into the frontend bundle so the browser
# can open the backend WebSocket. Must be the externally reachable URL.
API_URL=https://your-domain.com
# Port the container listens on (most hosts inject this automatically).
PORT=8080
# Internal Reflex backend port behind Caddy.
BACKEND_PORT=8000
# Set to 1 when the public domain is only known at deploy time.
REFLEX_EXPORT_ON_START=0

# --- OAuth callback -----------------------------------------------------------
# Used by app/states/auth_state.py to build https://<host>/auth/callback.
# Either set the base URL...
APP_BASE_URL=https://your-domain.com
# ...or pin the full callback URL explicitly (highest priority).
# OAUTH_REDIRECT_URL=https://your-domain.com/auth/callback

# --- Supabase -----------------------------------------------------------------
SUPABASE_URL=
SUPABASE_KEY=

# --- Google OAuth -------------------------------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- OpenAI -------------------------------------------------------------------
OPENAI_API_KEY=

# --- Database (optional) ------------------------------------------------------
REFLEX_DB_URL=
